name: Main OTA Update

on:
  push:
    branches:
      - main

jobs:
  ota_update:
    type: update
    params:
      platform: android
      channel: production
      message: "OTA update from main"
